import Testing
@testable import Categories

@Suite("Category Repository") struct CategoryRepositoryTests {
    var categoryRepository: CategoryRepository

    @Test func test() {
        #expect(true)
    }
}
